{% extends 'base.html.twig' %}

{% block title %}WordGroup{% endblock %}

{% block body %}
    <h1>WordGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ word_group.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ word_group.date ? word_group.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_word_group_index') }}">back to list</a>

    <a href="{{ path('app_word_group_edit', {'id': word_group.id}) }}">edit</a>

    {{ include('word_group/_delete_form.html.twig') }}
{% endblock %}
